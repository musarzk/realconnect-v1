# Production Environment Variables Template
# Copy this file and configure for your production environment
# NEVER commit actual production values to version control

# ============================================
# DATABASE CONFIGURATION
# ============================================
# MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
# Security: Use IP whitelist in MongoDB Atlas, strong password
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/?retryWrites=true&w=majority

# Database name (default: Realbase)
MONGODB_DB=Realbase

# ============================================
# AUTHENTICATION & SECURITY
# ============================================
# JWT Secret - MUST be a strong random string (32+ characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# CRITICAL: Never reuse development secrets in production
JWT_SECRET=your-generated-secret-here-minimum-32-characters

# ============================================
# APPLICATION CONFIGURATION
# ============================================
# Your production domain (no trailing slash)
# This will be your Vercel deployment URL or custom domain
NEXT_PUBLIC_API_URL=https://your-app.vercel.app

# Node environment
NODE_ENV=production

# ============================================
# PAYMENT INTEGRATION (Flutterwave)
# ============================================
# Optional: Only needed if using payment features
# Get credentials from: https://dashboard.flutterwave.com/
FLW_PUBLIC_KEY=FLWPUBK-your-public-key
FLW_SECRET_KEY=FLWSECK-your-secret-key
FLW_ENCRYPTION_KEY=FLWSECK-your-encryption-key
FLW_CLIENT_ID=your-client-id
FLW_CLIENT_SECRET=your-client-secret

# ============================================
# EMAIL SERVICE (Nodemailer)
# ============================================
# Optional: Only needed if using email notifications
# Configure for your email service provider
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=noreply@yourdomain.com

# ============================================
# CLOUD STORAGE (Cloudinary)
# ============================================
# Optional: Only needed if using image uploads
# Get credentials from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ============================================
# ANALYTICS & MONITORING
# ============================================
# Optional: Vercel Analytics (automatically enabled on Vercel)
# Optional: Add other analytics/monitoring services as needed
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# ============================================
# SECURITY NOTES
# ============================================
# 1. Never commit this file with real values
# 2. Use Vercel's environment variable dashboard for production
# 3. Rotate secrets regularly (especially JWT_SECRET)
# 4. Use different secrets for staging and production
# 5. Enable MongoDB IP whitelist for production servers only
# 6. Use strong, unique passwords for all services
